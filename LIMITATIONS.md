# LIMITATIONS — 做不到什么 / 需要人类帮什么（诚实清单）

> 写在这里的都是真话。宁可少说，不许编造。——依总纲 §29「最危险的十个问题」自查。

## 程序能力边界（现在做不到）

| # | 边界 | 说明 | 计划 |
|---|---|---|---|
| 1 | 扫描件/照片 OCR 默认不可用 | 默认只做 **PDF 文本层**提取（文字型 PDF 有效）。图片和扫描 PDF 需要 PaddleOCR（`pip install zhanzhen[ocr]` 自动检测，首次运行下载模型） | Week3 按 spec 封装 worker |
| 2 | 单租户 | MVP 只有租户上下文参数与数据目录隔离；**没有**登录/RBAC/RLS。别放真实客户数据上公网 | Week2 PostgreSQL+RBAC |
| 3 | 存储是本地目录+JSON快照 | 不是 MinIO/S3；并发写入未加锁，仅供单机使用 | Week2 对象存储 |
| 4 | 增值税发票**验真**做不到 | 国家税务系统无公开接口，本程序不做发票真伪联网查验，只做格式/金额平衡/重复检测 | 等官方开放或线下渠道 |
| 5 | 科目建议是规则模板 | 分录科目来自按凭证类型的映射模板，不是你的企业会计政策；确认前必须人工看 | 企业级映射配置 |
| 6 | AI 助手默认关闭 | 开启需配 `ZZ_AI_BASE_URL/KEY/MODEL`；只解释与建议，不改账。LLM 幻觉防线=schema 校验+无引用不回答，但**不是零幻觉保证** | 持续 |
| 7 | 规则只有 3 条 | 金额一致性/疑似重复/完整性。audit-os 的 12 条规则迁移是 W5+ 批次 | rules 表驱动 |
| 8 | XLSX 导出需 openpyxl | 未装则降级 CSV；HTML 报告+CSV 永远可用 | — |
| 9 | 无移动端 | 手机采集请配合 [audit-os-mobile](https://github.com/Ya-MiC/audit-os-mobile)；Flutter 化另立 ADR | — |
| 10 | 金标数据集为空 | spec 要求每类≥30张去敏样本——**这只能由人提供** | 见下节 |

## 需要人类（创始人）帮助的事

- [ ] **裁掉 ADR-001 三个勾选**（action-tree/docs/adr/ADR-001.md 还挂着，server 动工前必须）
- [ ] 提供 ≥30 张/类的**去敏**真实凭证样本（数电发票/银行回单/费用票），建立金标数据集
- [ ] 给出 1-2 家真实企业脱敏账套，用于 M0「端到端跑通+误报率记录」（audit-os Issue #4）
- [ ] 决定部署形态：先单机 Windows 给小所试用？还是直接上云？
- [ ] 确认 AI 助手用哪家免费端点（当前支持 NVIDIA integrate / OpenRouter，key 不入 Git）

## 明确不做（非目标，总纲 §14 / spec §14）

不取代注册会计师执业判断；不出具签字审计意见；不对政府系统自动申报；不用一个 prompt 取代数据模型+规则引擎+人工覆核。
